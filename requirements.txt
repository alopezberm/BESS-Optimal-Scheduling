# Core
numpy
pandas
matplotlib
requests            # Energinet API access in bess_opt.data

# Optimisation
gurobipy>=11        # requires a licence; the pip-bundled one is size-limited but sufficient here
pulp                # open-source fallback (bundles the CBC solver) - no licence needed

# Notebook execution
jupyter
ipykernel
